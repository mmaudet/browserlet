/**
 * BSL step parser for playback execution
 * Re-export shim: forwards to @browserlet/core/parser
 */

// Re-export from shared package
export { parseSteps, parseTimeout, validateStep, updateStepHints } from '@browserlet/core/parser';
