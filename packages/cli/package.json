{
  "name": "@browserlet/cli",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "browserlet": "./bin/browserlet.js"
  },
  "scripts": {
    "build": "npx tsx scripts/build-resolver.ts && tsc --build",
    "build:resolver": "npx tsx scripts/build-resolver.ts",
    "clean": "rm -rf dist",
    "dev": "tsc --build --watch"
  },
  "dependencies": {
    "@browserlet/core": "*",
    "classic-level": "^3.0.0",
    "commander": "^14.0.0",
    "env-paths": "^3.0.0",
    "ora": "^9.0.0",
    "picocolors": "^1.1.1",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
